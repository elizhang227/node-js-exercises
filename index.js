const messages = require('./messages');
const log = require('./logger');

console.log(messages);

log.info('Node is running');
log.info('Is nodemon working?');
// example of error case
// log.error('Everything is on fire');

app.get('/login', () => {
    
})